services:
  server:
    build: 
      context: .
      dockerfile: Dockerfile
    restart: always
    volumes:
      - ./images:/app/images
    # TODO: Fix this before production deployment
    ports:
      - 3000:3000
    depends_on:
      - db

